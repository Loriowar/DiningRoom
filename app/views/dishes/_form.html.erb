<%= form_for(@dish) do |f| %>
  <% if @dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dish.errors.count, "error") %> prohibited this dish from being saved:</h2>

      <ul>
      <% @dish.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <b>Тип блюда:</b><br />
      <%= f.select :dish_type_id, options_from_collection_for_select(DishType.all, :id, :name) %>
    </div>

    <div class="field">
      <b>Название:</b><br />
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <b>Калории:</b><br />
      <%= f.number_field :calories %>
      <b>ккал</b>
    </div>

    <div class="field">
      <b>Вес:</b><br />
      <%= f.number_field :weight %>
      <b>грамм</b>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
